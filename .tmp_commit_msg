chore(cleanup): trim unused scaffolding flagged by code review

* drop 6 empty stub packages (agent-trio/canvas/event-graph/memory/skill-loader/vendor) — only doc-comments, no implementation, zero external imports
* drop orphan skills/ directory (10 front-matter-only SKILL.md, used only by deleted skill-loader)
* drop TBD placeholders CHANGELOG.md / CLAUDE.md / AGENTS.md at repo root
* drop stale upstream tests/App.test.ts (open-design App exports no longer exist after NovelCut rewrite)
* fix pre-existing strict-mode error: targets[i] could be undefined in Storyboard batch loops
* README: replace misleading packages/* architecture tree with honest single-line + clarifying note pointing to apps/web/src/novelcut/ as the actual implementation site

Kept: apps/daemon (used by web app via providers/daemon.ts), packages/contracts+platform+sidecar+sidecar-proto (used by daemon), Tasks page (audit log actively written by Novel/Assets/Scripts/Storyboard)
